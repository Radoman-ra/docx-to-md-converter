# 🚀 Быстрый старт - Конвертер DOCX → Markdown

## Для вашего файла DiplomaThesis.docx

### ⚡ Самый простой способ (3 шага)

```bash
# 1. Установите pandoc (один раз)
brew install pandoc

# 2. Перейдите в папку с файлом
cd /Users/max/Downloads/diploma

# 3. Запустите конвертацию
./convert.sh DiplomaThesis.docx
```

**Готово!** У вас появятся:
- `DiplomaThesis.md` - ваш документ в Markdown
- `DiplomaThesis_images/` - все изображения из документа

---

## 🔄 Альтернативный способ (без pandoc)

```bash
# 1. Установите Python библиотеку
pip3 install python-docx

# 2. Запустите конвертацию
python3 convert_docx_to_md_python.py DiplomaThesis.docx
```

---

## 📝 Другие полезные команды

```bash
# Автопоиск .docx файла в папке
./convert.sh

# Конвертировать конкретный файл
python3 convert_docx_to_md.py DiplomaThesis.docx

# Конвертировать все .docx файлы
for file in *.docx; do ./convert.sh "$file"; done
```

---

## ❓ Проблемы?

**Ошибка "pandoc not found":**
```bash
brew install pandoc
```

**Ошибка "No module named 'docx'":**
```bash
pip3 install python-docx
```

**Ошибка "Permission denied":**
```bash
chmod +x convert.sh
```

---

## 📖 Подробная документация

- `SETUP.md` - детальная инструкция по установке
- `README_CONVERTER.md` - полная документация по всем функциям

---

**💡 Совет:** Для дипломной работы лучше использовать pandoc - он дает лучший результат!
