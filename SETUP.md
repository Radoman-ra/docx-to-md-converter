# Установка и запуск конвертера

## 🚀 Быстрая установка

### Вариант 1: С Pandoc (РЕКОМЕНДУЕТСЯ - лучшее качество)

```bash
# Установите pandoc
brew install pandoc

# Запустите конвертер
./convert.sh DiplomaThesis.docx
# или
python3 convert_docx_to_md.py DiplomaThesis.docx
```

### Вариант 2: Без Pandoc (чистый Python)

```bash
# Установите Python зависимости
pip3 install -r requirements.txt
# или
pip3 install python-docx

# Запустите конвертер
./convert.sh DiplomaThesis.docx
# или
python3 convert_docx_to_md_python.py DiplomaThesis.docx
```

### Вариант 3: Универсальный скрипт

```bash
# Просто запустите - сам определит, что использовать
./convert.sh DiplomaThesis.docx
```

## 📋 Что будет создано

После запуска `./convert.sh DiplomaThesis.docx` у вас появится:

```
DiplomaThesis.md              ← Markdown файл
DiplomaThesis_images/         ← Папка с изображениями
  ├── image1.png
  ├── image2.jpg
  └── ...
```

## 🔍 Проверка установки

Выполните эти команды, чтобы проверить, что установлено:

```bash
# Проверка pandoc
pandoc --version

# Проверка python-docx
python3 -c "import docx; print('✓ python-docx установлен')"
```

## 💻 Команды для разных ситуаций

### Конвертировать один файл
```bash
./convert.sh DiplomaThesis.docx
```

### Автоматически найти .docx в папке
```bash
./convert.sh
# или
python3 convert_docx_to_md.py
```

### Конвертировать все .docx файлы
```bash
for file in *.docx; do
    ./convert.sh "$file"
done
```

## ❗ Если что-то не работает

### "pandoc: command not found"
```bash
brew install pandoc
```

### "No module named 'docx'"
```bash
pip3 install python-docx
```

### "Permission denied"
```bash
chmod +x convert.sh
chmod +x convert_docx_to_md.py
chmod +x convert_docx_to_md_python.py
```

### Файл не найден
Убедитесь, что файл DiplomaThesis.docx находится в той же папке:
```bash
ls -la *.docx
```

## 🎯 Рекомендации

1. **Для диплома/важных документов**: Используйте pandoc (Вариант 1)
2. **Быстрая проверка**: Используйте универсальный скрипт `./convert.sh`
3. **Без установки pandoc**: Используйте python версию (Вариант 2)

## 📊 Сравнение методов

| Функция | Pandoc | Python-docx |
|---------|--------|-------------|
| Заголовки | ✅ Отлично | ✅ Хорошо |
| Таблицы | ✅ Отлично | ⚠️ Базово |
| Изображения | ✅ Да | ✅ Да |
| Формулы | ✅ Да | ❌ Нет |
| Списки | ✅ Да | ✅ Да |
| Установка | Требует Homebrew | Только pip |

## 🔧 Детальная настройка

### Изменить папку для изображений
Отредактируйте скрипт и измените строку:
```python
images_dir = f"{base_name}_images"  # Измените на нужное имя
```

### Изменить имя выходного файла
```python
output_md = f"{base_name}.md"  # Измените формат
```

## 📚 Полная документация

Смотрите `README_CONVERTER.md` для подробной документации.
