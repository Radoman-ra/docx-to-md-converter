# Конвертер DOCX в Markdown

Два скрипта для конвертации документов .docx в формат Markdown с сохранением изображений.

## 📋 Доступные скрипты

### 1. `convert_docx_to_md.py` (Рекомендуется)
**Использует:** Pandoc  
**Преимущества:** Лучшее качество конвертации, поддержка сложного форматирования  
**Требования:** pandoc

```bash
# Установка pandoc на macOS
brew install pandoc

# Использование
python convert_docx_to_md.py                    # Автоматически найдет .docx в папке
python convert_docx_to_md.py DiplomaThesis.docx # Указать файл явно
```

### 2. `convert_docx_to_md_python.py`
**Использует:** python-docx  
**Преимущества:** Не требует внешних программ, чистый Python  
**Недостатки:** Более простая конвертация  
**Требования:** python-docx

```bash
# Установка зависимостей
pip install python-docx

# Использование
python convert_docx_to_md_python.py                    # Автоматически найдет .docx
python convert_docx_to_md_python.py DiplomaThesis.docx # Указать файл явно
```

## 🚀 Быстрый старт

### Вариант 1: С Pandoc (лучшее качество)

```bash
# 1. Установите pandoc
brew install pandoc

# 2. Запустите скрипт
python convert_docx_to_md.py
```

### Вариант 2: Без Pandoc (чистый Python)

```bash
# 1. Установите библиотеку
pip install python-docx

# 2. Запустите скрипт
python convert_docx_to_md_python.py
```

## 📁 Структура вывода

После конвертации файла `DiplomaThesis.docx` будет создано:

```
DiplomaThesis.md              # Markdown файл
DiplomaThesis_images/         # Папка с изображениями
  ├── image1.png
  ├── image2.jpg
  └── ...
```

## 🔧 Особенности

### `convert_docx_to_md.py` (Pandoc)
- ✅ Отличная поддержка таблиц
- ✅ Сохранение списков (нумерованных и маркированных)
- ✅ Правильная обработка заголовков всех уровней
- ✅ Сохранение форматирования (жирный, курсив)
- ✅ Автоматическое извлечение изображений
- ✅ Поддержка формул (если есть в документе)

### `convert_docx_to_md_python.py` (Python)
- ✅ Не требует установки внешних программ
- ✅ Работает везде, где есть Python
- ✅ Извлечение изображений
- ⚠️ Упрощенная обработка таблиц
- ⚠️ Базовое форматирование

## 💡 Примеры использования

### Конвертация одного файла
```bash
python convert_docx_to_md.py MyDocument.docx
```

### Конвертация всех .docx в папке
```bash
for file in *.docx; do
    python convert_docx_to_md.py "$file"
done
```

### Конвертация с переименованием
```bash
python convert_docx_to_md.py input.docx
# Создаст input.md и input_images/
```

## ❓ Решение проблем

### Pandoc не найден
```bash
# macOS
brew install pandoc

# Ubuntu/Debian
sudo apt-get install pandoc

# Windows
# Скачайте с https://pandoc.org/installing.html
```

### python-docx не найден
```bash
pip install python-docx
# или
pip3 install python-docx
```

### Ошибка "Файл не найден"
Убедитесь, что:
1. Файл .docx находится в той же папке, что и скрипт
2. Или укажите полный путь: `python convert_docx_to_md.py /path/to/file.docx`

### Изображения не извлекаются
- Проверьте, что в документе действительно есть изображения
- Убедитесь, что у вас есть права на запись в текущую папку

## 🎯 Рекомендации

1. **Для важных документов**: Используйте `convert_docx_to_md.py` с pandoc
2. **Для простых документов**: Можно использовать `convert_docx_to_md_python.py`
3. **После конвертации**: Проверьте результат, особенно таблицы и формулы
4. **Изображения**: Автоматически сохраняются в отдельную папку с относительными путями

## 📝 Примечания

- Скрипты автоматически создают папку для изображений
- Если файл .md уже существует, он будет перезаписан
- Имена выходных файлов генерируются автоматически на основе имени входного файла
- Поддерживаются все стандартные форматы изображений (PNG, JPG, GIF, etc.)

## 📄 Лицензия

Эти скрипты предоставляются "как есть" для свободного использования.
